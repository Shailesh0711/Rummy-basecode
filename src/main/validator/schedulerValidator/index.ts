import waitingForPlayerSchedulerValidator from "./waitingForPlayerSchedulerValidator";
import initializeGameplaySchedulerValidator from "./initializeGameplaySchedulerValidator"
import bootAmountCollectSchedulerValidator from './bootAmountCollectSchedulerValidator'
import tossCardTSchedulerValidator from "./tossCardTSchedulerValidator";
import distributeCardsSchedulerValidator from "./distributeCardsSchedulerValidator";
import startTurnTSchedulerValidator from "./startTurnTSchedulerValidator"
import reShuffleCardsSchedulerValidator from "./reShuffleCardsSchedulerValidator";
import startFinishTimerValidator from "./startFinishTimerValidator"
import remainPlayersdeclarTimerValidator from "./remainPlayersdeclarTimerValidator"
import scoreBoardTimerValidator from "./scoreBoardTimerValidator";
import leaveTableTimerValidator from "./leaveTableTimerValidator"
import lockInPeriodTimerValidator from "./lockInPeriodTimerValidator"
import splitAmountTimerValidator from "./splitAmountTimerValidator";
import nextRoundTimerValidator from "./nextRoundTimerValidator";
import autoSplitTimerSchedulerValidator from "./autoSplitTimerSchedulerValidator";
import arrageSeatingSchedulerValidator from "./arrageSeatingSchedulerValidator";
import rejoinGamePopupValidator from "./rejoinGamePopupValidator";

const exportObject = {
    waitingForPlayerSchedulerValidator,
    initializeGameplaySchedulerValidator,
    bootAmountCollectSchedulerValidator,
    tossCardTSchedulerValidator,
    distributeCardsSchedulerValidator,
    startTurnTSchedulerValidator,
    reShuffleCardsSchedulerValidator,
    startFinishTimerValidator,
    remainPlayersdeclarTimerValidator,
    scoreBoardTimerValidator,
    leaveTableTimerValidator,
    lockInPeriodTimerValidator,
    splitAmountTimerValidator,
    nextRoundTimerValidator,
    autoSplitTimerSchedulerValidator,
    arrageSeatingSchedulerValidator,
    rejoinGamePopupValidator
}

export = exportObject